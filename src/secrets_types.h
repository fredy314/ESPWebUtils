#ifndef SECRETS_TYPES_H
#define SECRETS_TYPES_H

#include <IPAddress.h>

// Оголошення структури WiFiNetwork
struct WiFiNetwork {
  const char* ssid;
  const char* password;
};

#endif // SECRETS_TYPES_H
