#ifndef SECRETS_H
#define SECRETS_H

// Оголошення типів (структура WiFiNetwork)
#include "secrets_types.h"

// ============================================
// ОБОВ'ЯЗКОВІ НАЛАШТУВАННЯ
// ============================================

// WiFi мережі (в порядку пріоритету)
WiFiNetwork wifiNetworks[] = {
  {"YourSSID1", "YourPassword1"},
  {"YourSSID2", "YourPassword2"}
};
const int wifiNetworkCount = sizeof(wifiNetworks) / sizeof(wifiNetworks[0]);
#define WIFI_NETWORKS_DEFINED

// ============================================
// ОПЦІОНАЛЬНІ НАЛАШТУВАННЯ (можна не вказувати)
// ============================================

// Hostname для mDNS (якщо не вказано, згенерується з MAC адреси)
// Приклад: "smart-light" буде доступний як smart-light.local
#define WIFI_HOSTNAME "smart-light"

// Статичний IP (якщо не вказано, буде використано DHCP)
// Для DHCP закоментуйте або встановіть IPAddress(0, 0, 0, 0)
#define STATIC_IP IPAddress(192, 168, 0, 251)

// Автентифікація для /update
#define AUTH_PASSWORD "your-secure-password"

// Розкоментуйте та змініть за потреби:
// #define NTP_SERVER "ua.pool.ntp.org"
// #define GMT_OFFSET_SEC 7200
// #define DAYLIGHT_OFFSET_SEC 3600
// #define GATEWAY_IP IPAddress(192, 168, 0, 1)
// #define SUBNET_MASK IPAddress(255, 255, 255, 0)
// #define PRIMARY_DNS IPAddress(8, 8, 8, 8)
// #define SECONDARY_DNS IPAddress(8, 8, 4, 4)
// #define AUTH_REALM "My Light"
// #define AUTH_USERNAME "admin"
// #define AUTH_FAIL_MESSAGE "Access denied"

// Завантаження конфігурації з дефолтами
#include "secrets_config.h"

#endif // SECRETS_H
